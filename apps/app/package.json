{
  "name": "app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri",
    "lint": "prettier --check package.json src-tauri/tauri.conf.json src-tauri/tauri.macos.conf.json",
    "fix": "prettier --write package.json src-tauri/tauri.conf.json src-tauri/tauri.macos.conf.json",
    "test": "cargo check --manifest-path src-tauri/Cargo.toml"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.10.0",
    "prettier": "^3.8.1"
  }
}
